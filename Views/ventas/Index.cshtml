@model IEnumerable<Hardchord.Models.ventas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.userID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.totalPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.userID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.totalPrice)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ventasID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ventasID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ventasID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
